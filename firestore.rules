rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for all documents
    // WARNING: This is insecure and for development only.
    // You should restrict access based on user authentication in a real application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
