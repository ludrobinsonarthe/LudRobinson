rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: Allow read and write access to all authenticated users.
    // Specific data access is controlled by Firestore queries within the application code.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
