rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all authenticated users
    // THIS IS NOT A SECURE RULE. You should restrict access based on your app's needs.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
