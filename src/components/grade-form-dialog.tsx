// This component is no longer used and can be removed.
// The logic has been moved to an inline table editing format in `grade-management/page.tsx`.
// I am leaving the file here for now but emptying it to avoid confusion.
// We can delete it in a future step.

export default function GradeFormDialog() {
    return null;
}
